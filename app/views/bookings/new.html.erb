<%= render "layouts/navbar"%>
<div class="container mt-5">
  <h1>New Booking</h1>
  <%= simple_form_for [@celeb, @booking] do |f| %>
    <%= f.input :booking_date %>
    <%= f.input :accepted %>
    <%= f.submit, class:"btn btn-flat" %>
  <% end %>
</div>
